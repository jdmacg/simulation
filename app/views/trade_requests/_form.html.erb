<%= form_for(trade_request) do |f| %>

  <% if trade_request.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(trade_request.errors.count, "error") %> prohibited this trade_request from being saved:</h2>

      <ul>
      <% trade_request.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :outgoing_property %>
    <%= collection_select(:property_id, 0, Property.all, :id, :city) %>
  </div>

  <div class="field">
    <%= f.label :outgoing_cash %>
    <%= f.text_field :outgoing_cash %>
  </div>

  <!-- <div class="field">
    <%= f.label :offeror_id %>
    <%= f.text_field :offeror_id %>
  </div>

  <div class="field">
    <%= f.label :offeree_id %>
    <%= f.text_field :offeree_id %>
  </div>

  <div class="field">
    <%= f.label :response %>
    <%= f.check_box :response %>
  </div>

  <div class="field">
    <%= f.label :incoming_cash %>
    <%= f.text_field :incoming_cash %>
  </div>

  <div class="field">
    <%= f.label :incoming_property %>
    <%= f.text_field :incoming_property %>
  </div>

  <div class="field">
    <%= f.label :completed %>
    <%= f.check_box :completed %>
  </div> -->

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
